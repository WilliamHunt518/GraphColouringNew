--- +++ @@ -485,12 +485,14 @@ 
         try:
             # Build context
+            self.log("[REACT] Building context...")
             context = self._build_context()
             prompt = f"{self.react_prompt}\n\n{context}"
 
             # Add human message if received
             if self._received_human_message_this_turn:
                 human_msg = self._format_human_message()
+                self.log(f"[REACT] Human message: {human_msg[:100]}")
                 prompt += f"\n\n**Human message**: {human_msg}\n\nPlease respond to this message."
                 self._received_human_message_this_turn = False
 
@@ -498,6 +500,7 @@             trajectory = []
             backend_output = None
 
+            self.log(f"[REACT] Starting ReAct loop (max {self.max_react_iterations} iterations)")
             for iteration in range(self.max_react_iterations):
                 self.log(f"[REACT] Iteration {iteration + 1}/{self.max_react_iterations}")
 
@@ -507,6 +510,7 @@                     {"role": "user", "content": "\n".join(trajectory) if trajectory else "Begin reasoning."}
                 ]
 
+                self.log(f"[REACT] Calling backend LLM...")
                 response = self.backend_llm.chat.completions.create(
                     model=self.backend_model,
                     messages=messages,
@@ -519,6 +523,7 @@                 trajectory.append(thought_action)
 
                 self.log(f"[REACT] Thought+Action: {thought_action[:200]}...")
+                print(f"[{self.name}][REACT] Got LLM response: {thought_action[:100]}...")
 
                 # Check for final answer
                 if "Final Answer:" in thought_action:
@@ -588,11 +593,17 @@                     self._send_backend_decision(backend_output)
 
         except Exception as e:
-            self.log(f"[REACT] Error in ReAct reasoning: {e}")
+            self.log(f"[REACT] ERROR in ReAct reasoning: {e}")
             import traceback
-            self.log(traceback.format_exc())
-            # Fall back to algorithmic mode
-            super().step()
+            error_trace = traceback.format_exc()
+            self.log(f"[REACT] ERROR traceback:\n{error_trace}")
+            print(f"\n{'='*70}")
+            print(f"FATAL ERROR: ReAct agent failed")
+            print(f"Agent: {self.name}")
+            print(f"Error: {e}")
+            print(f"LLM_REACT mode requires working LLM - no fallbacks allowed")
+            print(f"{'='*70}\n")
+            raise SystemExit(f"ReAct agent FAILED: {e}") from e
 
     def _execute_action(self, action_name: str, action_args: str) -> Any:
         """Execute action given parsed name and arguments.
@@ -688,13 +699,32 @@             Observation result
         """
         # Parse action line
-        action_match = re.search(r"Action:\s*(\w+)\((.*?)\)", text, re.IGNORECASE)
-
-        if not action_match:
+        # CRITICAL FIX: Use parenthesis counting instead of non-greedy regex
+        # Non-greedy (.*?) stops at FIRST ), breaking dict args like {"h1": "red"}
+        action_start = re.search(r"Action:\s*(\w+)\(", text, re.IGNORECASE)
+
+        if not action_start:
             return {"error": "No valid action found in text"}
 
-        action_name = action_match.group(1)
-        action_args_str = action_match.group(2)
+        action_name = action_start.group(1)
+
+        # Find matching closing parenthesis by counting
+        start_pos = action_start.end()  # Position after opening (
+        paren_count = 1
+        i = start_pos
+
+        while i < len(text) and paren_count > 0:
+            if text[i] == '(':
+                paren_count += 1
+            elif text[i] == ')':
+                paren_count -= 1
+            i += 1
+
+        if paren_count != 0:
+            return {"error": "Unmatched parentheses in action"}
+
+        # Extract args between opening ( and matching closing )
+        action_args_str = text[start_pos:i-1]
 
         self.log(f"[REACT] Parsed action: {action_name}({action_args_str})")
 
@@ -708,8 +738,31 @@                 args_dict = json.loads(action_args_str)
             elif '=' in action_args_str:
                 # Keyword arguments: key=value, key2=value2
+                # CRITICAL: Don't split by comma naively - respect nested structures
                 args_dict = {}
-                for pair in action_args_str.split(','):
+
+                # Smart comma splitting that respects nesting
+                pairs = []
+                current_pair = ""
+                depth = 0  # Track nesting depth (braces, brackets, parens)
+
+                for char in action_args_str:
+                    if char in '{[(':
+                        depth += 1
+                    elif char in '}])':
+                        depth -= 1
+                    elif char == ',' and depth == 0:
+                        # Only split on commas at depth 0
+                        pairs.append(current_pair)
+                        current_pair = ""
+                        continue
+                    current_pair += char
+
+                if current_pair:
+                    pairs.append(current_pair)
+
+                # Parse each key=value pair
+                for pair in pairs:
                     if '=' in pair:
                         key, val = pair.split('=', 1)
                         key = key.strip()
@@ -722,7 +775,8 @@                                 args_dict[key] = json.loads(val)
                             else:
                                 args_dict[key] = val
-                        except:
+                        except Exception as parse_err:
+                            self.log(f"[REACT] Warning: Failed to JSON parse value for {key}: {parse_err}")
                             args_dict[key] = val
             else:
                 # Single positional argument
@@ -737,6 +791,16 @@             func = getattr(self.api, action_name)
             try:
                 result = func(**args_dict)
+
+                # CRITICAL: Wrap tuple returns as dicts for JSON serialization
+                # Some API functions return tuples (e.g., get_current_penalty returns (penalty, conflicts))
+                if isinstance(result, tuple):
+                    if action_name == "get_current_penalty" and len(result) == 2:
+                        result = {"penalty": result[0], "conflicts": result[1]}
+                    else:
+                        # Generic tuple wrapping
+                        result = {"result": list(result)}
+
                 return result
             except Exception as e:
                 error_msg = f"Error executing {action_name}: {str(e)}"
